import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['tailus.io', 'unsplash.com', 'images.unsplash.com', 'avatars.githubusercontent.com'],
  },
};

export default nextConfig;
